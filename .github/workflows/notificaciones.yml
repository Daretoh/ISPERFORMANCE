name: Notificaciones de tareas

on:
  schedule:
    - cron: '*/5 * * * *'   # cada ~5 minutos
  workflow_dispatch: {}       # tambien se puede correr a mano

jobs:
  enviar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm init -y && npm install web-push @supabase/supabase-js
      - run: node scripts/enviar-notificaciones.mjs
        env:
          SUPABASE_URL: https://fyjvcnqklgzrscgpytqj.supabase.co
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          VAPID_PUBLIC: BDt1m2YVzH-GB8MN4CF5ORzPhoEEBEJK3licTQH4K-oy_o1pXMMSbJl7HHbmeHY3XrOzUP-CvcNgz1V_J_CMupw
          VAPID_PRIVATE: ${{ secrets.VAPID_PRIVATE }}
          VAPID_SUBJECT: mailto:is.performance4x4@gmail.com
